import { Response } from "express";
import { AppProps } from "@app/types";
declare const buildReactTemplateStream: (res: Response, props?: AppProps, Component?: any) => void;
export { buildReactTemplateStream };
